/**
 * Shared form validation.
 *
 *   import { COMMON_SPECS, validateForSpec } from '@/utils/validation';
 *
 * See rules.js for the primitives and where each backend limit comes from,
 * and fieldSpecs.js for how a field label resolves to a rule.
 */
export * from './rules';
export * from './fieldSpecs';
