/**
 * Re-export of the shared error-code contract so app code imports it via the
 * `@/` alias. The canonical file is `doc/api_error_codes.js` (kept in sync with
 * the backend) — edit that one, not this re-export.
 */
export * from '../../doc/api_error_codes.js';
